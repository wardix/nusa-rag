{
  "name": "nusa-rag",
  "scripts": {
    "dev": "bun run --hot src/main.ts",
    "build": "bun build --target bun --sourcemap --minify --outfile dist/main.js src/main.ts"
  },
  "dependencies": {
    "@google/generative-ai": "^0.14.0",
    "hono": "^4.4.10",
    "nats": "^2.28.2",
    "surrealdb.js": "^1.0.0-beta.9"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
